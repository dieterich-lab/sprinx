# integration test paths: absolute paths only.
# not a secret file; contains filesystem paths, not credentials.
# safe to commit; add to .gitignore only if paths reveal sensitive infrastructure.
SPRINX_CANONICAL_CM=/absolute/path/to/TRNAinf-euk.cm
SPRINX_ARMLESS_CM_DIR=/absolute/path/to/truncated_cm/
